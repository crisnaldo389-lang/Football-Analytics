[pytest]
# La racine du dépôt doit être importable : les tests chargent helpers.py et main.py
pythonpath = .
testpaths = tests
filterwarnings =
    # Streamlit signale l'absence de contexte de session quand on appelle st.metric hors app
    ignore::UserWarning
